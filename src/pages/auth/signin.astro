---
import Layout from "@/layouts/Layout.astro";
import SignInForm from "@/components/auth/SignInForm";

const authErrorMessages = {
  invalid_credentials: "Nieprawidłowy adres e-mail lub hasło.",
  supabase_not_configured: "Supabase nie jest skonfigurowany.",
} as const;

const errorCode = Astro.url.searchParams.get("error");
const error =
  errorCode && errorCode in authErrorMessages ? authErrorMessages[errorCode as keyof typeof authErrorMessages] : null;
---

<Layout title="Logowanie">
  <div class="bg-auth-garden flex min-h-screen items-center justify-center p-4">
    <div class="garden-card w-full max-w-sm rounded-xl border p-8 text-emerald-950 backdrop-blur-md">
      <p class="mb-2 text-center text-sm font-semibold tracking-[0.25em] text-emerald-700 uppercase">Planer ogrodu</p>
      <h1 class="mb-6 text-center text-2xl font-bold text-emerald-950">Zaloguj się</h1>
      <SignInForm serverError={error} client:only="react">
        <p
          slot="fallback"
          class="rounded-sm border border-emerald-200 bg-white/70 px-3 py-2 text-sm text-emerald-900/70"
        >
          Ładowanie formularza logowania…
        </p>
      </SignInForm>
      <p class="mt-4 text-center text-sm text-emerald-900/70">
        Nie masz konta? <a href="/auth/signup" class="font-semibold text-emerald-700 hover:underline">Utwórz konto</a>
      </p>
    </div>
  </div>
</Layout>
